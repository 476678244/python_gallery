import { ChatWorkspace } from "@/features/workspace";

/**
 * SafeClaw Agent Workspace
 * 
 * Business-first, Domain-driven Architecture
 * - Features organized by business capability
 * - Entities as domain models
 * - Services for API layer
 * - Stores for state management
 */
export default function Home() {
  return <ChatWorkspace />;
}
